<style>
        .issue {
            padding: 5px 8px;
            margin: 4px;
            border-radius: 14px;
            border: 2.5px solid {{ colors.border }};
        }
        
        .issue-header {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 8px;
        }
        
        .status-icon {
            height: 16px;
            border-radius: 10px;
            padding: 5px 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: {{ fonts.small }}px;
            flex-shrink: 0;
        }
        
        .status-open {
            background-color: {{ colors.success }};
            color: {{ colors.text }};
        }
        
        .status-closed {
            background-color: {{ colors.warning }};
            color: {{ colors.text }};
        }
        
        .issue-number {
            font-size: {{ fonts.medium }}px;
            font-weight: 600;
            color: {{ colors.primary }};
        }
        
        .issue-title {
            font-size: {{ fonts.big }}px;
            color: {{ colors.secondary }};
            line-height: 1.4;
        }
        
        .labels {
            display: flex;
            flex-wrap: wrap;
            gap: 4px;
        }
        
        .label {
            padding: 4px 12px;
            border-radius: 12px;
            font-size: {{ fonts.tiny }}px;
            font-weight: 600;
            white-space: nowrap;
        }
      </style>
      {% if animation_duration > 0 %}
      <style>
        @keyframes issue-appear {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
      </style>
      {% endif %}
      {% for issue in issues %}
      <div class="issue" {% if animation_duration > 0 %}style="animation: issue-appear {{ animation_duration }}s ease both; animation-delay: {{ loop.index0 * animation_duration }}s;"{% endif %}>
          <div class="issue-header">
              <div class="status-icon {% if issue.state == 'open' %}status-open{% else %}status-closed{% endif %}">
                  {% if issue.state == "open" %}open{% else %}close{% endif %}
              </div>
              <span class="issue-number">#{{ issue.number }}</span>
              <div class="issue-title">
                  {{ issue.title }}
              </div>
          </div>
          
          {% if issue.labels %}
          <div class="labels">
              {% for label in issue.labels %}
              <span class="label" 
                    style="color: {{ label.color|color_hex }}; border: 2px solid {{ label.color|color_hex }}; background-color: {{ label.color|color_hex }}40;">
                  {{ label.name }}
              </span>
              {% endfor %}
          </div>
          {% endif %}
      </div>
      {% endfor %}